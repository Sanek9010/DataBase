<div class="container-fluid border border-dark col-centered bg-light" id="faculty">
    {% if (is_granted('IS_AUTHENTICATED_FULLY')==false) %}

    {% else %}
        <br />

        {{ form_start(form2) }}

        Выберите факультет из списка:
        {{ form_widget(form2.nameoffaculty) }}

        {{ form_end(form2) }}

        Или добавьте новый:

        {{ form_start(form) }}
        Название факультета:
        {{ form_widget(form.nameoffaculty) }}
        {{ form_errors(form.nameoffaculty) }}

        Код факультета:
        {{ form_widget(form.codeoffaculty) }}
        {{ form_errors(form.codeoffaculty) }}

        {{ form_widget(form.submit) }}

        {{ form_end(form) }}

        <br />
        <br />
    {% endif %}

</div>