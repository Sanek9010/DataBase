<div class="container-fluid border border-dark col-centered bg-light" id="faculty">
    {% if (is_granted('IS_AUTHENTICATED_FULLY')==false) %}

    {% else %}
        <br />

        {{ form_start(form2) }}

        Выберите направление из списка:
        {{ form_widget(form2.nameofspecialty) }}

        {{ form_end(form2) }}

        Или добавьте новое:

        {{ form_start(form) }}
        Название направления:
        {{ form_widget(form.nameofspecialty) }}
        {{ form_errors(form.nameofspecialty) }}

        Код направления:
        {{ form_widget(form.codeofspecialty) }}
        {{ form_errors(form.codeofspecialty) }}

        <button type="submit">Register!</button>
        {{ form_end(form) }}

        <br />
        <br />
    {% endif %}

</div>